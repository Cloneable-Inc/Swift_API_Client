//
// GetMetrics200ResponseWorkflows.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetMetrics200ResponseWorkflows: Codable, JSONEncodable, Hashable {

    public var currentDurationMinutes: Double
    public var currentItemCount: Double
    public var prevDurationMinutes: Double
    public var prevItemCount: Double
    public var durationDelta: Double
    public var itemCountDelta: Double
    public var currentItemFinishedCount: Double
    public var prevItemFinishedCount: Double
    public var itemFinishedCountDelta: Double

    public init(currentDurationMinutes: Double, currentItemCount: Double, prevDurationMinutes: Double, prevItemCount: Double, durationDelta: Double, itemCountDelta: Double, currentItemFinishedCount: Double, prevItemFinishedCount: Double, itemFinishedCountDelta: Double) {
        self.currentDurationMinutes = currentDurationMinutes
        self.currentItemCount = currentItemCount
        self.prevDurationMinutes = prevDurationMinutes
        self.prevItemCount = prevItemCount
        self.durationDelta = durationDelta
        self.itemCountDelta = itemCountDelta
        self.currentItemFinishedCount = currentItemFinishedCount
        self.prevItemFinishedCount = prevItemFinishedCount
        self.itemFinishedCountDelta = itemFinishedCountDelta
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case currentDurationMinutes = "current_duration_minutes"
        case currentItemCount = "current_item_count"
        case prevDurationMinutes = "prev_duration_minutes"
        case prevItemCount = "prev_item_count"
        case durationDelta = "duration_delta"
        case itemCountDelta = "item_count_delta"
        case currentItemFinishedCount = "current_item_finished_count"
        case prevItemFinishedCount = "prev_item_finished_count"
        case itemFinishedCountDelta = "item_finished_count_delta"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(currentDurationMinutes, forKey: .currentDurationMinutes)
        try container.encode(currentItemCount, forKey: .currentItemCount)
        try container.encode(prevDurationMinutes, forKey: .prevDurationMinutes)
        try container.encode(prevItemCount, forKey: .prevItemCount)
        try container.encode(durationDelta, forKey: .durationDelta)
        try container.encode(itemCountDelta, forKey: .itemCountDelta)
        try container.encode(currentItemFinishedCount, forKey: .currentItemFinishedCount)
        try container.encode(prevItemFinishedCount, forKey: .prevItemFinishedCount)
        try container.encode(itemFinishedCountDelta, forKey: .itemFinishedCountDelta)
    }
}

