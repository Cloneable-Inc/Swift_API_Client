//
// BulkUpdateJobStatusRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct BulkUpdateJobStatusRequest: Sendable, Codable, ParameterConvertible, Hashable {

    public enum Status: String, Sendable, Codable, CaseIterable {
        case pending = "pending"
        case inProgress = "in_progress"
        case completed = "completed"
        case cancelled = "cancelled"
    }
    public var jobIds: [UUID]
    public var status: Status

    public init(jobIds: [UUID], status: Status) {
        self.jobIds = jobIds
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case jobIds
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(jobIds, forKey: .jobIds)
        try container.encode(status, forKey: .status)
    }
}

